[tox]
envlist = {py37,py38}

[testenv]
commands = coverage run -m unittest discover tests
           codecov -e TOXENV
deps =
    moto
    pyftpdlib
    codecov
passenv =
    CI
    TRAVIS
    TRAVIS_*